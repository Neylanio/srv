<%= form_with(model: user_server, local: true) do |form| %>
  <% if user_server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_server.errors.count, "error") %> prohibited this user_server from being saved:</h2>

      <ul>
      <% user_server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_server_user_id %>
  </div>

  <div class="field">
    <%= form.label :server_id %>
    <%= form.text_field :server_id, id: :user_server_server_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
